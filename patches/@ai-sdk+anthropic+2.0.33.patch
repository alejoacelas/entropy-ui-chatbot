diff --git a/node_modules/@ai-sdk/anthropic/dist/index.mjs b/node_modules/@ai-sdk/anthropic/dist/index.mjs
index c5b8286..3287c6a 100644
--- a/node_modules/@ai-sdk/anthropic/dist/index.mjs
+++ b/node_modules/@ai-sdk/anthropic/dist/index.mjs
@@ -1697,7 +1697,27 @@ function mapAnthropicStopReason({
 // src/anthropic-messages-language-model.ts
 function createCitationSource(citation, citationDocuments, generateId3) {
   var _a;
+  console.log('[ANTHROPIC] createCitationSource called with type:', citation.type);
+  // Handle web search citations
+  if (citation.type === "web_search_result_location") {
+    const citationObj = {
+      type: "citation",
+      id: generateId3(),
+      citedText: citation.cited_text,
+      title: citation.title,
+      url: citation.url,
+      providerMetadata: {
+        anthropic: {
+          encryptedIndex: citation.encrypted_index
+        }
+      }
+    };
+    console.log('[ANTHROPIC] Created web search citation:', citationObj);
+    return citationObj;
+  }
+  // Handle document citations
   if (citation.type !== "page_location" && citation.type !== "char_location") {
+    console.log('[ANTHROPIC] Skipping citation type:', citation.type);
     return;
   }
   const documentInfo = citationDocuments[citation.document_index];
@@ -2600,13 +2620,17 @@ var AnthropicMessagesLanguageModel = class {
                   }
                   case "citations_delta": {
                     const citation = value.delta.citation;
+                    console.log('[ANTHROPIC] citations_delta received:', citation);
                     const source = createCitationSource(
                       citation,
                       citationDocuments,
                       generateId3
                     );
                     if (source) {
+                      console.log('[ANTHROPIC] Enqueuing citation/source:', source);
                       controller.enqueue(source);
+                    } else {
+                      console.log('[ANTHROPIC] createCitationSource returned nothing');
                     }
                     return;
                   }
diff --git a/node_modules/@ai-sdk/anthropic/dist/internal/index.js b/node_modules/@ai-sdk/anthropic/dist/internal/index.js
index c378b52..f5d92cf 100644
--- a/node_modules/@ai-sdk/anthropic/dist/internal/index.js
+++ b/node_modules/@ai-sdk/anthropic/dist/internal/index.js
@@ -1673,6 +1673,21 @@ function mapAnthropicStopReason({
 // src/anthropic-messages-language-model.ts
 function createCitationSource(citation, citationDocuments, generateId2) {
   var _a;
+  if (citation.type === "web_search_result_location") {
+    return {
+      type: "source",
+      sourceType: "url",
+      id: generateId2(),
+      url: citation.url,
+      title: citation.title,
+      providerMetadata: {
+        anthropic: {
+          citedText: citation.cited_text,
+          encryptedIndex: citation.encrypted_index
+        }
+      }
+    };
+  }
   if (citation.type !== "page_location" && citation.type !== "char_location") {
     return;
   }
diff --git a/node_modules/@ai-sdk/anthropic/dist/internal/index.mjs b/node_modules/@ai-sdk/anthropic/dist/internal/index.mjs
index 759e9b1..8b24c4d 100644
--- a/node_modules/@ai-sdk/anthropic/dist/internal/index.mjs
+++ b/node_modules/@ai-sdk/anthropic/dist/internal/index.mjs
@@ -1683,6 +1683,21 @@ function mapAnthropicStopReason({
 // src/anthropic-messages-language-model.ts
 function createCitationSource(citation, citationDocuments, generateId2) {
   var _a;
+  if (citation.type === "web_search_result_location") {
+    return {
+      type: "source",
+      sourceType: "url",
+      id: generateId2(),
+      url: citation.url,
+      title: citation.title,
+      providerMetadata: {
+        anthropic: {
+          citedText: citation.cited_text,
+          encryptedIndex: citation.encrypted_index
+        }
+      }
+    };
+  }
   if (citation.type !== "page_location" && citation.type !== "char_location") {
     return;
   }
